{
  "name": "hugo-starter",
  "version": "1.0.0",
  "description": "A simple starter for Hugo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "hugo server --watch --disableFastRender",
    "clean": "rm -rf public",
    "build": "hugo --verbose"
  },
  "author": "Harry Cresswell",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^9.7.1",
    "postcss-cli": "^6.1.3"
  }
}
